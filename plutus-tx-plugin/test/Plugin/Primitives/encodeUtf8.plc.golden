(program
  (let
    (nonrec)
    (termbind
      (strict)
      (vardecl encodeUtf (fun (con string) (con bytestring)))
      (builtin encodeUtf8)
    )
    (termbind
      (strict)
      (vardecl encodeUtf (fun (con string) (con bytestring)))
      (lam s (con string) [ encodeUtf s ])
    )
    [ encodeUtf (con string "abc") ]
  )
)